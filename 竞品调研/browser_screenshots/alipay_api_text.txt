6
⌥
code
code
遇到问题？让我来为你解答～
去咨询
文档中心
小程序 
介绍
开发
工具
设计
数据
运营
更新日志
控制台
登录
开发
指南
框架
组件
API
服务端
小程序云
支付宝小程序备案
小程序 API 概览
小程序 API 使用说明
端权限接口使用类目说明
基础 API
开放能力 API
支付
用户授权
会员
获取会员基础信息
获取会员收货地址
获取会员手机号
my.getPhoneNumber
周期扣款
商家会员卡
消息
模板配置
支付宝卡包
交易组件
小程序商品
小程序前端 API 错误码对照表
开发 > API > 开放能力 API > 会员 > 获取会员手机号 > my.getPhoneNumber
my.getPhoneNumber
更新时间：2026-01-12 17:46:45
收藏
我的文档
设置

my.getPhoneNumber(Object object)

基础库 1.16.4 或更高版本；

支付宝客户端 支持

支小宝客户端 支持

安诊儿客户端 支持

以 Promise 风格调用： 不支持 。

主体： 企业支付宝小程序

相关文档： button 组件 、 接口内容加密方式 、 接口加签方式
简介

获取用户支付宝绑定的手机号。

此功能需谨慎使用，若支付宝发现信息存在超出约定范围使用或者不合理使用等情况，支付宝有权永久回收接口权限。

效果示例
获取用户手机号时，会先经过用户授权，可点击左侧的箭头展开查看C端界面。用户也可从小程序右上角胶囊按钮的“···”打开授权详情界面，解除授权。

未授权时弹出

点击右上角胶囊按钮的“···”查看授权详情

打开设置页面

进入用户信息可解除授权

接入流程
获取手机号
为了确保准确接入“获取手机号”的能力， 需要按照 配置加密加签 > 用户授权 > API调用 > 解密及验签 的流程接入。
1
配置加密加签
开放平台
2
用户授权
小程序
3
API调用
小程序
4
解密及验签
服务端

是否必须： 是。

加密：开放平台-开发设置 设置 接口内容加密方式 （可参考文档接口内容加密方式）。

加签： 如需验证内容的真实性，可进一步设置接口加签方式：开放平台-开发设置 设置 接口加签方式 （可参考文档接口加签方式）。

未设置接口加密方式而直接调用 my.getPhoneNumber，将得到异常响应“缺少加密配置”（code 40001），可点此查看示例：未配置接口内容加签方式。

入参
Object object

查看示例

属性	类型	默认值	必填	描述
success	Function	-	否	

调用成功的回调函数


fail	Function	-	否	

调用失败的回调函数


complete	Function	-	否	

调用结束的回调函数（调用成功、失败都会执行）

success 回调函数
参数
Object res

查看示例

	属性	类型	描述
	response	String	

完整的响应数据，前端需要将该报文发送到开发者服务端做验签和解密处理。
通过 JSON.parse 解析后，属性如下：



属性	类型	描述
response	String | object	

报文。通常为一串加密字符串，经服务端解密后返回一个对象。
注意：在 code = 40001 的情况下，返回明文对象。详见示例代码 未配置接口内容加密方式


sign	String	

对 response 报文的签名

错误码

response 明文返回的异常报错信息（40001），以及 response 密文经由服务端解密后返回的异常报错信息。

结果码	结果码信息	解决方案
20000	系统繁忙	可提示用户“稍后再试”
40001	缺少加密配置	调用my.getPhoneNumber之前，需完成接口内容加密方式的配置： 进入 开放平台-开发设置 配置 接口内容加密方式 。详见：接口内容加密方式
40001	应用未设置默认签名类型	进入 开放平台-开发设置 配置 应用网关 。详见：应用网关
40002	加密异常	参考文档：接口内容加密方式，重新设置接口内容加密方式
40003	无效的授权关系	获取支付宝会员手机号需要用户进行授权，授权行为通过 <button> 组件 的 点击 动作来触发， <button> 组件open-type 的值设置为 getAuthorize 并将 scope 设为 phoneNumber。 用户点击并同意之后，可以通过 my.getPhoneNumber 接口获取到支付宝会员加密后的手机号

代码示例
通过 <button> 授权
my.getPhoneNumber(Object object)
success参数
解密后正确的返回值
未正确通过 <button> 组件授权
接口内容加密有误
未配置接口内容加密方式
<button
  open-type="getAuthorize"
  scope="phoneNumber"
  onGetAuthorize="getPhoneNumber"
  onError="handleAuthError"
>
  授权手机号
</button>


授权行为通过 <button> 组件 的 点击 动作来触发，唤起授权框。

Bugs & Tips
Tip
谨慎使用，若支付宝发现信息存在超出约定范围使用或者不合理使用等情况，支付宝有权永久收回该小程序的接口权限。
Tip
my.getPhoneNumber 只用于获取手机号，如需可靠地识别用户身份，建议使用 my.getAuthCode 并通过 OpenAPI 获取 uid 或者进一步的用户信息。
Tip
my.getPhoneNumber 仅支持企业小程序调用，个人小程序调用会报错误码 40006。
常见问题
Q：调用 my.getPhoneNumber，报错 “无效的授权关系”，如何处理？
A：

请参考本文接入流程-获取手机号部分的用户授权。也可通过 my.getSetting 接口返回的 phoneNumber 判断用户是否授权过手机号，phoneNumber 为 true 即已授权。

Q：调用 my.getPhoneNumber，报错 “缺少加密配置”，如何处理？
A：

请参考本文接入流程-获取手机号部分的配置加密加签。

Q：为什么调用 my.getPhoneNumber 返回的手机号为空？
A：

一般情况下都是当前用户的支付宝账号没有绑定手机号所致，需要当前用户登录 账号管理，绑定手机号。

 上一篇：my.getAddress
下一篇：my.paySignCenter 
相关问题
获取会员手机号
获取会员手机号
申请获取会员手机号驳回理由不明
获取会员手机号申请一直是在审核中
内容没有解决您的问题？您还可以前往 自助服务平台 或 支付宝开放社区 寻求帮助
关于蚂蚁集团诚征英才联系我们International BusinessAbout Alipay
Copyright © 支付宝（杭州）数字服务技术有限公司  | ICP证浙B2-20160559
该文档对你是否有帮助？
反馈
当前页面目录
my.getPhoneNumber(Object object)
简介
接入流程
入参
错误码
代码示例
Bugs & Tips
常见问题