# ========================================
# 美柚会员订阅后台活动管理系统 - 环境变量配置
# ========================================
# 复制此文件为 .env.local 并填入实际值：
#   cp .env.example .env.local

# ---------- Supabase 数据库（必填）----------
# 在 https://supabase.com 创建项目后获取
# Settings → API → Project URL / Project API keys
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---------- 服务端口（可选）----------
# 本地开发时使用，部署到 Vercel/Netlify 时由平台自动管理
# DEPLOY_RUN_PORT=5000

# ========================================
# 部署方式
# ========================================
#
# 方式一：Vercel（推荐）
#   1. 在 supabase.com 创建项目，执行 supabase-init.sql 建表
#   2. 在 vercel.com 导入 GitHub 仓库
#   3. 配置上述 3 个环境变量
#   4. Deploy
#
# 方式二：Netlify
#   1. 在 supabase.com 创建项目，执行 supabase-init.sql 建表
#   2. 在 netlify.com 导入 GitHub 仓库
#   3. Build command: pnpm build
#   4. Publish directory: .next
#   5. 配置上述 3 个环境变量（Site settings → Environment variables）
#   6. Deploy
#
# 方式三：自有服务器（Docker / Node.js）
#   1. 在 supabase.com 创建项目，执行 supabase-init.sql 建表
#   2. git clone 代码仓库
#   3. cp .env.example .env.local && 填入环境变量
#   4. pnpm install && pnpm build && pnpm start
